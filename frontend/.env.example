# URL pública del API Gateway (sin barra final). En Render se define en render.yaml.
VITE_API_URL=https://coworkspace-gateway.onrender.com
# Para desarrollo local con docker compose:
# VITE_API_URL=http://localhost:3000
